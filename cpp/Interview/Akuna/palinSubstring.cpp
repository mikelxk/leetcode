#include "../../std.hpp"
/*
 * Complete the 'getMaxSubstrings' function below.
 *
 * The function is expected to return an INTEGER.
 * The function accepts following parameters:
 *  1. STRING s
 *  2. INTEGER k
 *  The length of every substring is greater than or equal to k.
 *  Each substring is a palindrome.
 *  No two substrings are the overlap
 *  Determine the maximum number of valid substrings that can be   formed from s.
 */
// not finished!
int getMaxSubstrings(string s, int k)
{
    int n = s.size();
    
}
int main()
{
    string s = "ababaeocco";
    int k = 4;
    auto ret = getMaxSubstrings(s, k);
    //
    cout << ret;
    
}
